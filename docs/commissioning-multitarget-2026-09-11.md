# 2026-09-11：跨启动质量策略修复与多目标前台测试

## 授权与边界

操作员确认屋顶完全打开、天空晴朗、设备旁无人，授权修复、安装、重启、回零和
多类型目标真实测试。通过后台桥触发前台同一 ICommand，不使用 computer use，
不复制设备控制或采集路径。运行中只读观察；发生阻断先保留证据，再修复或显式
结束运行。天亮、持续云层或操作员收口时停止后续目标。

本轮继续使用已部署的旧测光服务，不在活动观测中切换 QHY 所有者。结果不能作为
双 N.I.N.A. 测光端或无人值守验收。每次科学曝光仍需真实目标/狭缝/导星证据与
实际光谱质量检查；精度警告不是精确入缝成功。

## 首次失败与修正

0.4.0.167 的 `UVEX-20260911T113313Z-cbe78a8e4b11478` 保持 10 Lac 目标，
但 N.I.N.A. 重启后临时精度警告授权为 false。19:36 定位完成后，24 组完成窗口
没有通过严格 2 px 门，返程并重新解算后报 `PHD2_LOCK_INHERITED_BUDGET_EXHAUSTED`。
账本是本轮 19:37:18 新建，并非前夜用量：使用 6/8 次、52.35/100 px，耗尽的是
300 s 时限。未取得光谱，原记录保留。

0.4.0.168 按操作员要求和 [ADR-0015](adr/0015-persistent-supervised-slit-quality-policy.md)：

- 明确选过的质量策略保存到当前 Profile，顶部可见；新配置仍默认严格。
- 策略被锁入相同运行配置哈希，并单列 manifest 标签。
- 后台设备控制权仍只在当前进程有效，不由质量策略授予。
- 完成等待用尽时仍执行原有核验回程；确认后保留具体质量/时间原因和残差，
  不再将其转成会耗时的全场重建。原动作、位移、时间及回程预算不变。

原运行正常取消后，`nina-home-20260911T120713Z-661a217a` 两次新读回确认
AtHome、Tracking=false、Slewing=false。此为运行外维护，不改写失败记录。

## 构建与部署

完整构建和 1,402 项 .NET 测试通过；26 个界面场景渲染并检查。输出在忽略目录
`tmp/ui-168`。源码检查对 CRLF/LF 做归一化，仍要求选星循环之后、发导星命令之前
重新验证标定，不删除或放松该检查。

20:15 安装 `.168`，交付 DLL SHA-256：
`11B3A18A2AA8E40E265946A1B99C8D8FE95B5D6C570F859789715BB2FDA9C70F`。
七个插件依赖均与构建产物一致，安装前插件和 Profile 已备份；不重启 PHD2/UVEX
服务，不变更台站模板。维护记录：`plugin-0.4.0.168-20260911T121521Z`。

N.I.N.A. PID 20916 中通过可见命令保存警告策略，然后正常退出并重新打开：
PID 24376 读回 `supervisedSlitQualityWarningAuthorized=true`，但
`realControlArmedForThisNinaSession=false`。界面错误为空，新 N.I.N.A. 日志没有
XAML/Binding/dispatcher/ERROR。仅以现有 API 打开成像工作区；接口不提供独立
AvalonDock 子面板激活，校准库等三面板真实逐个实例化仍单列待验，不能用离线图冒充。

## 10 Lac 复测（通过）

`UVEX-20260911T121642Z-985d539799ef44c` 于 20:16:42 通过正式后台桥单次
`restart-real-run` 启动。先读回已保存质量策略，再重新授予本次设备控制权。
20:18 进入 PHD2 入缝，20:19 三个新帧残差为 2.63、3.93、4.20 px，正式产生
`PHD2_SLIT_PRECISION_WARNING_PROBE_AUTHORIZED`。目标身份和照明狭缝证据仍受检，
没有使用模型侧旁路；没有因严格 2 px 完成窗口耗尽而重新阻断。

前台达到 Completed、11/11，控制器
`frontend-model-loop-20260911T121641Z-0c73c9d3` 核验成功，模型修复次数为 0。
0.1 s、30 s 探针保留后自动升档，120 s 探针通过。3 张正式曝光均为 120 s，
实际接受 3/3；管线连续谱 SNR 分别为 598.95、898.42、818.83，谱迹削顶列比例均为 0。
另有 68 张同步测光和 2 张广域定位帧，不能将 70 张都称为测光帧。

独立只读核对了 6 张光谱 FITS 的 SHA-256、目标、运行/捕获标识、曝光、质量标志及
二维数据，读取前后哈希一致。科学帧 `SLITRES` 为 4.2924、2.4730、3.4153 px，
`SLITWARN=True`；属于有人监督工程接受，不证明精确入缝或流量定标精度。

完成后以 `nina-home-20260911T123229Z-7d68269e` 再次确认回零、跟踪关闭，
原运行和原始数据不变。下一轮通过同一目标草稿导入按钮切换到天津四 Deneb。

## Deneb 第一轮与邻场边界修复

`UVEX-20260911T123346Z-788dccf2b04b404`（`.168`）20:33:46 启动。
WCS 确实多次成功，不应描述为“完全不能解算”。初始目标在探测器外，
邻场规划将投影放在 y=1620；新 WCS 得到 (1776.5,1621.4)。由于只允许
约 1e-9 的数学边界误差，下一步又选了几乎同一邻场，实际命令仅 0.667 arcsec，
随后“没有改善”分支返程。邻场搜索换一个位置后，20:39 成功到达狭缝附近，
进入 8/11 光谱选档；这证明原恢复路径有出口，但无效往返仍是软件缺陷。

`.169` 使用原有 2 arcsec 命令到位容差除以当帧正式 WCS 像元尺度，仅为
邻场/最终段选择提供边界滞回；异常大的输入至多取照明避让边界的四分之一。
不扩大光学身份、入缝容差、指向单步或累计预算；最终段仍按当帧 WCS 计算、
原账本预记账并读回。新增四边界及远场/异常输入测试，运行证据记录该像素容差。

本轮另出现光谱相机温度持续读回 0°C、目标 -10°C、CoolerOn=true、功率约
72.13%，尚无探针或科学曝光。没有接受这种遥测为到温。第一次取消请求因
前台已推进而返回 STALE_REVISION、没有动作；取得新状态后于 20:45 前正常取消。
导星和同步测光按取消流程停止，原始记录保留。不能将本轮标为完成。

`nina-home-20260911T124525Z-6c98c06e` 确认 AtHome、Tracking=false、Slewing=false。
20:46 安装 `.169`，1,407 项 .NET 测试通过，插件 SHA-256 为
`5C83B7F13808A5C817B85959967E8116776FAE394E5D1FA85597720F4D226265`。
维护记录 `plugin-0.4.0.169-20260911T124619Z`：原相机正常断开，N.I.N.A. 正常退出，
新 PID 25364；没有重启 PHD2 或 UVEX 服务，没有变更台站模板或原始数据。

## Deneb 复测（通过）

`.169` 的 `UVEX-20260911T124705Z-cfdcf4fd78204f0` 于 20:47:05 启动，
正式前台自行重连相机并设定 -10°C。实际温度从 -3.3°C 降到 -10.3°C 后稳定，
科学帧均记录 -10.0°C。连接瞬间 N.I.N.A. 日志仍有一条 ToupTek 初始 set point
1000 写入失败，随后实际 -10°C 指令和连续读回通过；不能据此声称日志全无错误，
也没有更换 SDK/驱动或将固定 0°C 当作到温。

本轮无邻场搜索，3 次原预算内 WCS 修正完成入场。连续精调残差 4.81、1.83、0.51 px
按保存的监督策略警告后试拍；0.1 s 升至 3 s 探针，最终 3×3 s 科学帧接受 3/3。
管线连续谱 SNR 641.44、1077.84、905.84，谱迹削顶列为 0；科学帧 `SLITRES`
为 0.9918、3.5649、0.6055 px，均保留 `SLITWARN=True`。

前台 Completed 11/11，控制器 `frontend-model-loop-20260911T124705Z-b48242c7`
Succeeded=true、模型修复次数 0；独立只读核对全部 5 张光谱 FITS 及哈希。
7 张同步测光帧保留但因 `STAR_DETECTION_CAPPED` 不计为合格测光，另有 2 张合格
广域定位帧。这是光谱前台闭环通过，不是测光质量验收通过。

## 邻场改善量补充修正（.170）

同一段代码还把“下一帧到狭缝的剩余总运动量”和“刚走过的邻场短步”相比较，
例如总余量从 700 降到 600 arcsec、只走了 100 arcsec 时，会错误判为没有改善。
`.170` 在邻场替换逆解之前保存原总余量，比较同一物理量的前后值，仍要求改善超过
原到位容差；新增有限值、负值、阈值边界及源码衔接测试。未增加任何运动权限或预算。

完整构建及 1,416 项 .NET 测试通过，冻结设计哈希检查通过。在已完成运行之外，
`nina-home-20260911T125329Z-afde7823` 确认回零和关闭跟踪，再正常安装重启。
20:54 的维护记录为 `plugin-0.4.0.170-20260911T125426Z`，新 PID 16708，插件
SHA-256 `FB3BBD8037C2A50EB28F987069701F41BDB143F272BF131117CD6E11835072A2`；
七个交付 DLL 与构建产物逐一匹配，原 Profile/插件已备份，台站模板哈希未变。
重启后质量策略仍为 true，设备控制授权为 false；目标从 Deneb 经同一可见草稿按钮
切到 Scheat，再独立授予本次真实控制权启动。前面的成功/取消运行不改写。

## Scheat（通过）

`.170` 的 `UVEX-20260911T125454Z-a4c3bc2c1d2b416` 于 20:54:54 启动，
21:02 完成 11/11，控制器 `frontend-model-loop-20260911T125453Z-67ed9f0f`
核验成功，模型修复次数 0。起始 5/10/15 s 解算阶梯未成功，正式恢复随后取得
新 WCS、重新居中并完成精调；没有由模型猜图授权目标位置。

自动选档从 0.1 s 升到 15 s，15 s 实测谱迹削顶列比例 0.4109375，保留为
`SATURATION_BACKOFF` 失败探针；回退到 3 s 新探针通过，再接受 3×3 s 正式光谱。
连续谱 SNR 为 277.22、412.11、469.45，正式谱迹削顶列均为 0。科学帧残差
2.0159、0.9724、3.0648 px、温度 -9.9°C；监督精度警告保留。
独立核对全部 6 张光谱 FITS 的目标、运行/捕获标识、曝光、数据及哈希。
同步测光 10 张接受，另有 2 张广域定位帧。饱和探针没有计为合格科学曝光。

## Gamma Cas 首轮：前台完成，但独立温度验收不通过

`.170` 的 `UVEX-20260911T130412Z-7afe4dc15dc8424` 于 21:04:12 启动，
控制器 `frontend-model-loop-20260911T130412Z-3a2486bc` 记录 Completed 11/11、
科学帧接受 3/3。10 s 探针因谱迹削顶列 0.00390625 回退到 3 s，三张正式谱迹均未削顶。
但是独立读取最后一张 FITS 发现 `CCD-TEMP=0`，而本轮目标为 -10°C；前两张为
-9.9°C。因此本轮不能列为完整验收成功，前台/控制器的旧成功记录保留而不改写。
相机在收尾后仍 Connected=true、CoolerOn=true，温度 0°C，功率约 70.49%，
不是断开相机后的默认值。同步测光另有 10 张、广域定位 2 张。

本机对应 N.I.N.A. 源码 `ToupTekAlikeCamera.Temperature` 忽略
`sdk.get_Temperature` 的返回状态，然后返回 out 值除以 10，存在将 SDK 读失败呈现为
0°C 的可能。本次没有直接 SDK 返回码，因此不能证明实际芯片在 0°C、不能断言硬件
损坏，也不能把问题完全归于驱动：插件在保存后只检查身份和光谱指标，是实际漏检。

`.171` 在 N.I.N.A. 报告保存完成后，只读检查实际 FITS `CCD-TEMP`/`SET-TEMP`
以及同一物理相机的保存后制冷读回；沿用原温度/设定点容差。两条证据均通过才能
增加探针或科学接受计数。异常文件仍经原保存路线保留，发布
`ATR_SAVED_FRAME_TEMPERATURE_INVALID` 及实测值，结束本阶段，不用曝光前缓存
改写 FITS 或补一张伪造成功，也不会将温度异常当成曝光太长而盲目试拍。
补充原始温度、缺字段、非有限值、设定点、保存后断言及实际计数顺序测试。
回零记录 `nina-home-20260911T131123Z-7ac3e9ed` 已确认，不改变这轮原始记录。

21:18 正常安装重启 `.171`，维护记录 `plugin-0.4.0.171-20260911T131840Z`，
新 PID 8644，实际插件 SHA-256 为
`A582FC3639EC808E061CC688BBC9F7711522D3883F79DC06106D952A8987BBF0`。
完整构建通过；补充中文错误文案断言后，最终全量 1,427 项 .NET 测试通过，零失败/跳过。
七个交付 DLL 哈希已核对，台站模板未变。另将只读独立 FITS 验收加入实际温度和
设定点检查，它明确拒绝了 Gamma Cas 首轮第三张，而不是仅依赖 manifest 接受计数。

## Gamma Cas 复测（.171，通过）

`UVEX-20260911T131910Z-1d23bf0605394e0` 于 21:19:10 经同一可见前台命令启动。
控制器 `frontend-model-loop-20260911T131910Z-fa03d124` 核验成功，正式前台
Completed 11/11，模型修复次数 0。0.1 s 探针后升至 15 s，实测削顶列比例
0.003125；自动回退到 10 s 仍为 0.00260417，继续回退到 3 s 新探针才通过。
两个饱和探针均保留且不计为接受，不人为跳过曝光选择。

21:24:33、21:24:44、21:24:54 的 3×3 s 正式光谱全部接受。连续谱 SNR
为 302.17、101.17、138.95，正式谱迹削顶列均为 0。三张科学帧 `SLITRES`
为 0.8616、0.5189、1.5572 px，整轮监督精度警告仍保留，没有事后改成精确入缝。
实际 FITS `CCD-TEMP` 分别为 -10.0、-9.9、-10.0°C，`SET-TEMP` 均为 -10.0°C；
三张证据均新增 `temperatureQualityCode=ATR_SAVED_FRAME_TEMPERATURE_VERIFIED`，
保存后相机读回和文件内温度同时通过。同步测光 13 张接受，另有 2 张广域定位帧。

独立只读核对全部 7 张光谱 FITS，并对 10 Lac、Deneb、Scheat 三个成功运行重新
执行包含温度/设定点的严格核对，均通过；读取前后 SHA-256 不变。没有改写旧版
Gamma Cas 首轮的错误接受记录，它仍单列为独立验收失败，不能计入以下合格总数。

## 本轮结果与停止状态

| 目标与测试类型 | 实际测试版本 | 正式前台 | 合格科学曝光 | 同步测光质量 |
| --- | --- | --- | --- | --- |
| 10 Lac，O 型热星 | .168 | Completed 11/11 | 3×120 s | 68 张接受 |
| Deneb，A 型亮星 | .169 | Completed 11/11 | 3×3 s | 7 张保留但不合格，星点检测上限 |
| Scheat，M 型红巨星 | .170 | Completed 11/11 | 3×3 s | 10 张接受 |
| Gamma Cas，Be 发射线星 | .171 | Completed 11/11 | 3×3 s | 13 张接受 |

合计 12 张经独立身份、哈希、温度和光谱指标核对的合格科学帧。各目标是在逐步修复
过程中的上述版本通过，不能声称四个目标均已在最终 .171 重跑。四类目标已完成本轮
光谱前台工程测试，因此不再启动新目标；不将 Deneb 的测光质量失败隐藏为双路成功。
只读生成并检查 `tmp/night-20260911/completed-spectra-qa.png`，包含上述四个成功
运行的二维谱迹与像素域诊断提取；它不是波长/流量定标产品。

21:29 的 `nina-home-20260911T132919Z-154b89f8` 两次新读回确认 AtHome=true、
Tracking=false、Slewing=false，PHD2 处于 Stopped/Selected。随后通过前台同源
`disarm-real-control` 解除本次后台真实设备授权，已保存的监督质量策略不变。
N.I.N.A. 保持运行，光谱相机无曝光；平场板读回 Closed、LightOn=false，屋顶
读回 ShutterOpen。本次没有关顶、退出设备所有者或断电，不等同于全台关机。

最终安装版本 .171、N.I.N.A. PID 8644。剩余边界：温度遥测偶发异常的底层 SDK
返回码尚未取得，修复的是本插件保存后漏检，不证明底层驱动永久正常；未验证双
N.I.N.A. 测光所有权、无人值守、绝对测光/流量定标及三个独立 AvalonDock 面板。
源码、测试与文档保留在 main 的工作区；本轮未创建额外分支，也未自动提交或推送。
