#!/bin/sh
set -eu

repo_root="$(git rev-parse --show-toplevel)"
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$repo_root/scripts/verify-design-baseline.ps1" -Quiet
git diff --cached --check
